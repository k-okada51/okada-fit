// `analyze-meal` のプロンプト（FEAT-08 §3.2 の `MEAL_ANALYZE_PROMPT`）。
//
// ⚠️ **設計書に本文が無い。** FEAT-08 §10 #9 が「未確定・🟡中」として残していた
// 箇所である。基線は PoC の実測プロンプト（`poc/food-vision/src/config.mjs`）。

/// 食事写真から栄養4項目を推定させる指示。
///
/// ## PoC の文言から1点だけ変えている（ADR-0026）
///
/// PoC は「この写真は日本のコンビニまたはファストフード・外食チェーンの商品です」
/// と**断定していた**。データセットが外食23チェーンだけだったためである。
///
/// 本番では自炊も撮る。断定を残すと、家庭料理を外食の既製品として推定し、
/// 実際とかけ離れた値になる。**断定を外し、両方を想定させる文にした。**
///
/// ⚠️ **ADR-0001 の実測 MAPE（外食14.7%・家庭料理18.8%）は旧文言のもの**である。
/// この文言では測っていない。再ベンチは ADR-0026 の残課題に置いた。
export const MEAL_ANALYZE_PROMPT =
  'この写真は日本の食事です。' +
  'コンビニ・ファストフード・外食チェーンの商品の場合もあれば、家庭料理の場合もあります。' +
  '写真に写っている食事1食分の栄養成分を、見た目・種類・量から推定してください。';
