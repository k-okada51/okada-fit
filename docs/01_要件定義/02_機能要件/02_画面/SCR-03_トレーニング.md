---
status: draft
---

# SCR-03 トレーニング — 画面概要

> **目的**: 1つの画面の概要——目的・操作主体・要素と導線・状態別表示・関連FEAT/IF——を1枚で示す。モックの正本は `src/mocks/{画面ID}.tsx`（→ `00_画面モック規約.md`）。
> **書き方**: 実データは書かず `{ }` を自プロジェクトの語に置き換える。参照は必ずID（`FEAT-*`／`EXT-*`／`ST-*`／`ERR-*`）。1ファイル＝1画面。画面の数だけ本テンプレートを複製する（`画面概要_テンプレート.md` → `SCR-01_{画面名}.md` 等）。記入例は example-suido-fax の対応ファイル参照。

## 目次
1. [概要](#1-概要)
2. [要素・操作導線](#2-要素操作導線)
3. [状態別表示](#3-状態別表示)
4. [入力・バリデーション](#4-入力バリデーション)
5. [関連FEAT・IF／モック](#5-関連friфモック)

## 1. 概要
> 📝 ここに画面の概要を記載。{画面ID／画面名／操作主体（アクター）／内容・役割／新規画面か既存画面への集約か}

| 項目 | 内容 |
|---|---|
| 画面ID | SCR-03 |
| 画面名 | トレーニング |
| 操作主体 | 岡田さん |
| 内容・役割 | 鍛えたい部位を選んで器具を絞り込み、generateでAIメニューを提案させ、実施結果（回数・重量・メニュー）を記録する |
| 新規/集約 | 新規 |

## 2. 要素・操作導線
> 📝 ここに主要な要素と操作導線を記載。{要素（検索・一覧・ボタン等）とその配置／主要導線（この画面からどこへ遷移するか・遷移先画面ID）}

- 部位セレクタ（胸/背中/脚/肩/腕）→ 選択部位に一致する器具一覧を表示（FEAT-02）。
- [generate]ボタン → AI（Claude）が当日メニューを提案し表示（FEAT-03）。
- 記録フォーム（回数・重量・メニュー）＋[記録]ボタン（FEAT-04）。
- 導線: 記録後は SCR-01 へ。器具未登録なら SCR-02 へ誘導。

## 3. 状態別表示
> 📝 ここに状態別の表示を記載。空／読込中／エラー／権限なし の各表示を明示。状態・エラーは `02_設計/60_テスト設計/02_RED母集合_受入基準・状態・エラー.md` の ST/ERR を ID参照。

| 状態 | 表示 | 関連ST/ERR |
|---|---|---|
| 空 | 該当部位の器具が無い場合は器具登録（SCR-02）へ誘導 | → `02_設計/60_テスト設計/`（設計で定義） |
| 読込中 | generate中はスピナー（AI生成待ち） | → `02_設計/60_テスト設計/`（設計で定義） |
| エラー | AI生成失敗時に再試行を提示 | → `02_設計/60_テスト設計/`（ERR、設計で定義） |
| 権限なし | 該当なし（単一ユーザのため） | — |

## 4. 入力・バリデーション
> 📝 ここに入力項目とバリデーションを記載。{入力項目／必須・任意／検証条件（文字数・形式・列挙）／違反時の挙動（ERR-ID）}

| 入力項目 | 必須 | 検証条件 | 違反時（ERR） |
|---|---|---|---|
| 鍛えたい部位 | 必須 | 胸/背中/脚/肩/腕 から1つ選択 | → 設計で定義 |
| 回数 | 必須 | 正の整数 | → 設計で定義 |
| 重量 | 必須 | 0以上の数値 | → 設計で定義 |

## 5. 関連FEAT・IF／モック
> 📝 ここに関連ID とモックの所在を記載。含める項目: 関連FEAT-ID／関連EXT-ID／モック(tsx)の場所／人間承認の状態（`00_画面モック規約.md` のゲート）。

| 項目 | 内容 |
|---|---|
| 関連FEAT | FEAT-02, FEAT-03, FEAT-04 |
| 関連IF | EXT-01（Claude） |
| モック(tsx) | `src/mocks/SCR-03.tsx` |
| 人間承認 | ☐ 承認（承認者・日付） |
