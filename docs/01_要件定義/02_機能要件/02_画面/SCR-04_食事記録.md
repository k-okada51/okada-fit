---
status: draft
---

# SCR-04 食事記録 — 画面概要

> **目的**: 1つの画面の概要——目的・操作主体・要素と導線・状態別表示・関連FEAT/IF——を1枚で示す。モックの正本は `src/mocks/{画面ID}.tsx`（→ `00_画面モック規約.md`）。
> **書き方**: 実データは書かず `{ }` を自プロジェクトの語に置き換える。参照は必ずID（`FEAT-*`／`EXT-*`／`ST-*`／`ERR-*`）。1ファイル＝1画面。画面の数だけ本テンプレートを複製する（`画面概要_テンプレート.md` → `SCR-01_{画面名}.md` 等）。記入例は example-suido-fax の対応ファイル参照。

## 目次
1. [概要](#1-概要)
2. [要素・操作導線](#2-要素操作導線)
3. [状態別表示](#3-状態別表示)
4. [入力・バリデーション](#4-入力バリデーション)
5. [関連FEAT・IF／モック](#5-関連friфモック)

## 1. 概要
> 📝 ここに画面の概要を記載。{画面ID／画面名／操作主体（アクター）／内容・役割／新規画面か既存画面への集約か}

| 項目 | 内容 |
|---|---|
| 画面ID | SCR-04 |
| 画面名 | 食事記録（タンパク質トラッキング） |
| 操作主体 | 岡田さん |
| 内容・役割 | 食事を撮影してAI（Claude）にタンパク質量を計算させ、当日の残量と、残量を補う食事（食事マスタから抽出）を表示する |
| 新規/集約 | 新規 |

## 2. 要素・操作導線
> 📝 ここに主要な要素と操作導線を記載。{要素（検索・一覧・ボタン等）とその配置／主要導線（この画面からどこへ遷移するか・遷移先画面ID）}

- 食事写真の撮影／アップロード（FEAT-08）。
- 計算結果（推定タンパク質量）と当日の摂取合計・残量の表示（FEAT-09）。
- 残量を補う食事の提示リスト（食事マスタから抽出・AI不使用）。
- 導線: 記録後は SCR-01 へ。食事マスタ未登録なら SCR-05（設定）へ誘導。

## 3. 状態別表示
> 📝 ここに状態別の表示を記載。空／読込中／エラー／権限なし の各表示を明示。状態・エラーは `02_設計/60_テスト設計/02_RED母集合_受入基準・状態・エラー.md` の ST/ERR を ID参照。

| 状態 | 表示 | 関連ST/ERR |
|---|---|---|
| 空 | 本日の食事記録なしのメッセージと撮影導線。食事マスタ未登録なら SCR-05 へ誘導 | → `02_設計/60_テスト設計/`（設計で定義） |
| 読込中 | 画像解析中（AI計算待ち）のスピナー | → `02_設計/60_テスト設計/`（設計で定義） |
| エラー | 画像解析失敗時に再試行を提示 | → `02_設計/60_テスト設計/`（ERR、設計で定義） |
| 権限なし | 該当なし（単一ユーザのため） | — |

## 4. 入力・バリデーション
> 📝 ここに入力項目とバリデーションを記載。{入力項目／必須・任意／検証条件（文字数・形式・列挙）／違反時の挙動（ERR-ID）}

| 入力項目 | 必須 | 検証条件 | 違反時（ERR） |
|---|---|---|---|
| 食事写真 | 必須 | 画像形式（jpg/png 等）・サイズ上限 `[仮]` | → 設計で定義 |

## 5. 関連FEAT・IF／モック
> 📝 ここに関連ID とモックの所在を記載。含める項目: 関連FEAT-ID／関連EXT-ID／モック(tsx)の場所／人間承認の状態（`00_画面モック規約.md` のゲート）。

| 項目 | 内容 |
|---|---|
| 関連FEAT | FEAT-08, FEAT-09 |
| 関連IF | EXT-01（Claude・画像入力） |
| モック(tsx) | `src/mocks/SCR-04.tsx` |
| 人間承認 | ☐ 承認（承認者・日付） |
