---
status: draft
---

# FEAT-03 AIメニュー提案 — 機能概要

> **目的**: 1つの機能要件（FEAT）の概要——目的・操作主体・入出力・関連FEAT/IF——を1枚で示す。**受入基準（Given-When-Then）は本書に書かず**、テスト設計をFEAT番号で参照する。
> **書き方**: 実データは書かず `{ }` を自プロジェクトの語に置き換える。参照は必ずID（`FEAT-*`／`EXT-*`／`ST-*`／`ERR-*`）。1ファイル＝1機能。FEATの数だけ本テンプレートを複製する（`01_機能概要_テンプレート.md` → `FEAT-01_{機能名}.md` 等）。記入例は example-suido-fax の対応ファイル参照。

## 目次
1. [概要](#1-概要)
2. [入出力概要](#2-入出力概要)
3. [関連FEAT・IF](#3-関連friф)
4. [受入基準・状態・エラー（参照）](#4-受入基準状態エラー参照)

## 1. 概要
> 📝 ここに機能の概要を記載。{FEAT-ID／機能名／優先度（MUST/NICE）／確定状態（[確定]/[暫定]）／目的（この機能で何が変わるか）／操作主体（アクター）}

| 項目 | 内容 |
|---|---|
| FEAT-ID | FEAT-03 |
| 機能名 | AIトレーニングメニュー提案（generate） |
| 優先度 | MUST |
| 確定状態 | [確定] |
| 目的 | generateボタン押下で、選択部位と絞り込んだ登録器具をもとにAI（Claude）が当日のトレーニングメニュー（種目・器具）を提案する。自分で決めると毎回同じメニューになりがちな課題を解消する。 |
| 操作主体（アクター） | 岡田さん（トリガ）／Claude（生成・EXT-01） |

## 2. 入出力概要
> 📝 ここに入力・出力の概要を記載（振る舞いのみ。DB・API・アルゴリズムなど手段は設計層へ委譲）。{入力（誰が何を）／出力（何が・どこへ）／トリガ（起点イベント）}

| 区分 | 内容 |
|---|---|
| 入力 | 選択部位・絞り込んだ器具（FEAT-02の結果）をClaudeへ渡す |
| 出力 | 提案トレーニングメニュー（種目・使用器具）を画面に表示 |
| トリガ | generateボタン押下（オンデマンド。自動/定期の提案は行わない） |

## 3. 関連FEAT・IF
> 📝 ここに関連するID を記載。含める項目: 前提FEAT／後続FEAT／関連EXT-ID／関連する業務ルール（RULE-ID）。他ドキュメントで定義したIDと厳密に一致させる。

| 区分 | 関連ID |
|---|---|
| 前提FEAT | FEAT-02 |
| 後続FEAT | FEAT-04 |
| 関連EXT | EXT-01（Claude） |
| 関連RULE | RULE-006 |

## 4. 受入基準・状態・エラー（参照）
> 📝 ここは記入不要（参照の明示）。本FEATの**受入基準（Given-When-Then）・状態機械（ST）・エラー母集合（ERR）**は、TDDのRED源泉として `02_設計/60_テスト設計/02_RED母集合_受入基準・状態・エラー.md` に **本FEAT番号で** 定義する。テスト観点は同フォルダ `03_テスト観点表_テンプレート.md` を参照。

- 受入基準・ST・ERR: → `02_設計/60_テスト設計/02_RED母集合_受入基準・状態・エラー.md`（FEAT-03）
- テスト観点: → `02_設計/60_テスト設計/03_テスト観点表_テンプレート.md`
