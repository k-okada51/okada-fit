---
status: draft
---

# 用語定義（用語・命名・記法の正本）

> **目的**: 全ドキュメント共通の読み方・命名・ID規約を定め、用語ゆらぎ・解釈ゆらぎを防ぐ。**本書は全層の正本**とし、全層からここを参照する。
> **書き方**:（記入例は example-suido-fax の対応ファイル参照）実データは書かず、用語／定義／別称禁止・ID付番規則の枠だけ用意する。用語は「正」を1つ決め、別称・禁止語を明記する。

## 目次
1. [ドキュメントの読み方](#1-ドキュメントの読み方)
2. [意思決定タグ](#2-意思決定タグ)
3. [ID付番規則](#3-id付番規則)
4. [用語集](#4-用語集)

## 1. ドキュメントの読み方
> 📝 ここに参照・矛盾解決のルールを記載。{参照はID（位置参照禁止）／矛盾時の解釈順位（トレーサビリティ ＞ 契約 ＞ 散文）}

- 参照は必ず **ID** で行う（位置参照禁止）。
- 矛盾時の解釈順位: {トレーサビリティ表 ＞ 各契約 ＞ 散文}。

## 2. 意思決定タグ
> 📝 ここに文中で使う意思決定タグを記載。{`[確定]`/`[暫定]`/`[未決→設計]`/`[未確定→確認待ち]` 等・🔧DESIGN＝設計で埋める枠}

| タグ | 意味 | 使いどころ |
|---|---|---|
| `[確定]` | 確定した内容 | FEATの確定状態など |
| `[暫定]` / `[仮]` | 暫定確定（設計/実装で確定させる前提）。**CIは不備扱いしない** | 技術スタック・目標値の仮置き |
| `⚠️ 要確認（人間判断）` | 人間の判断・確定が必要 | 認証方式・食事写真ポリシー等（CIが検出） |
| `⚠️ 未提供` | 値の入力元（件数・単価・工数など）が未提供 | 予算・従量費など |
| `DEC-xx` | 設計で確定＝ADR化する決定事項 | `01_全体像/05_未決定事項.md`・`意思決定_ADR/` |
| `TODO-xx` | 外部確定待ち／コスト系の未決事項 | 差別化調査・従量費など |
| `<!-- TODO:aic -->` | 未記入マーカー（記入待ちの空欄） | 未確定の担当・ID等 |
| `🔒` | 非公開・秘匿（先方/クライアント不可視） | APIキー・DB資格情報・非公開DB |
| `📝` / `🔧DESIGN` | テンプレのガイド行／設計で埋める枠 | 各テンプレ由来（削除・改変しない） |

## 3. ID付番規則
> 📝 ここにID接頭辞と付番ルールを記載。IDは**不変・欠番再利用禁止**。本記法は共通ハーネス(aic-harness)の要件レビュー(criteria.json)に準拠。

- **ハーネスID接頭辞（aic-harness/criteria.json 準拠）**: `FEAT-`（機能）/`ST-`（状態）/`TR-`（状態遷移）/`ERR-`（エラー）/`SCR-`（画面）/`RPT-`（帳票）/`EXT-`（外部連携）/`RULE-`（業務ルール）/`TASK-`（タスク）。
- **受入基準**＝`- [AC] Given <前提> When <操作> Then <期待>`（各 FEAT- にぶら下げる＝TDDのRED源泉・collect.py の測定対象）。
- **未記入マーカー**＝`<!-- TODO:aic -->`（記入待ちの空欄に置く）。
- **未決事項**＝各件に「オーナー」「解決フェーズ(設計/実装/運用)」を持たせる（`01_要件定義/01_全体像/05_未決定事項.md`）。
- **文書ステータス（システム的に管理）**＝**「mainにマージ済み＝承認済み」**（mainのブランチ保護＝PR＋メンバー承認 で担保。承認印は手書きしない）。先頭 frontmatter の `status:` は**「未完成のまま載せる」例外宣言だけ**に使う（`draft`＝書きかけ／`ai-reviewed`＝AI独立レビュー済／`wip`）。**status宣言が無い（＝承認相当）文書に `📝`／`{…}`／`<!-- TODO:aic -->`／`[仮]`／`🔧DESIGN` が残っていてはならない**（CIが検出・次工程AIが未確定値を実装に使う事故を防ぐ）。承認履歴（誰が/いつ）は **PR/git が正**（in-doc手書き不要）。
- **設計側の独自ID（harness観点外）**: `NFR-/DM-/DEC-/TC-/REQ-/KPI-`（我々の設計層で用いる補助ID。ハーネスの要件レビュー対象外）。
- IDは**不変・欠番再利用禁止**。

> 本PJでの採番実績: `REQ-01〜05`／`FEAT-01〜10`／`SCR-01〜05`／`EXT-01`／`RULE-001〜007`／`KPI-01〜03`／`TASK-001〜016`／`UC-01〜03`／`NFR-*`。`DEC-` は 区分別に `DEC-B01〜B06`（要件で洗い出し設計で確定）・`DEC-D01〜D04`（設計判断）を用いる。`TODO-` は `TODO-A0x`（外部確定待ち）・`TODO-C0x`（コスト系）。

> 命名規約（物理命名：DB/enum/API/JSON/エラーコード/サービス）は `../02_設計/50_詳細設計/06_DB設計規約.md` を正（将来は共通ハーネス運用へ）。

## 4. 用語集
> 📝 ここに用語を記載。1用語につき「正」を1つ決め、別称・禁止語を明記する。{用語（正）／定義／別称・禁止語・備考}

| 用語（正） | 定義 | 別称/禁止語・備考 |
|---|---|---|
| 岡田さん | 本システムの唯一の利用者（主アクター）。発注者＝実装者を兼ねる | 別称: ユーザ／利用者（文脈上は可） |
| 器具 | ジムのトレーニング機器。各器具に部位タグを付与する（`FEAT-01`） | — |
| 部位 | 鍛える対象の筋肉部位。**胸・背中・脚・肩・腕** の5種（`DEC-B04`） | 別称: 筋肉部位。禁止: 6種以上の独自区分 |
| 部位タグ | 器具に付与する部位（上記5種）のタグ。絞り込み・メニュー提案の基盤 | 別称禁止: 筋肉タグ／マッスルタグ |
| （AI）メニュー提案 | generateボタン押下で、**Claude**が選択部位のトレーニングメニュー（種目・器具）を提案すること（`FEAT-03`） | 禁止: ローテーション提案／自動提案（不使用） |
| generate（ボタン） | メニュー提案を起動する操作。オンデマンド（自動/定期ではない） | — |
| トレーニング記録 | 実施した **回数・重量・メニュー** の記録（`FEAT-04`） | 禁止（記録項目）: 部位・器具・セットのみ、等の旧定義 |
| ダッシュボード | 履歴・前回実施・タンパク質残量サマリ等の可視化（`FEAT-05`・`SCR-01`） | — |
| 必要タンパク質量 | 1日に必要なタンパク質量。**体重×2g**（`RULE-001`・`FEAT-07`） | 別称: 目標タンパク質量 |
| 摂取量 | その日に摂取したタンパク質量の合計（食事撮影から加算） | — |
| 残量 | **必要量 − 摂取量**（`RULE-002`・`FEAT-09`） | 別称: 不足量 |
| 食事マスタ | 食品×タンパク質量の対応データ。**CSVで初期取り込み**（`FEAT-10`・スキーマ `DEC-B05`） | 別称: 食品マスタ（可） |
| 不足分の食事提示 | 残量を補う食事を**食事マスタから抽出・提示**すること。**AI不使用**（`RULE-005`・`FEAT-09`） | 禁止: AIによる食事メニュー生成（対象外） |
| 目標ログイン回数 | 継続の目標値。既定 **月12回**（週3想定）（`FEAT-06`・`TODO-A02`） | — |
| Claude | 採用したAI（Anthropic、**画像入力対応**）。用途は①メニュー提案②食事写真のタンパク質計算の2箇所（`DEC-B01`・`EXT-01`） | 別称: Claude API／AI生成エンジン |
| 食事写真 | タンパク質計算のためClaudeへ送信する食事の画像（`FEAT-08`）。送信/保持は `DEC-D04` | — |

### スコープ外（用語として使うが、本PJでは「やらない」を明示）
- **目標体型**（ダイエット/減量/増量）に基づく **食事メニュー生成** … 対象外（`01_全体像/03_制約・前提.md` §2）
- **3日ルール／回復日数／ローテーション提案** … 用いない（同上）
