---
status: draft
---

# バッチ設計 — {バッチ名/対象処理}

> **目的**: 定期実行・非同期処理（ポーリング・再送・集計・アーカイブ等）の実装設計を、トリガ・処理内容・例外/リトライ・多重制御まで実装が迷わない粒度で固定する。
> **書き方**:（記入例は example-suido-fax の対応ファイル参照）実データは書かず、`{ }` を自プロジェクトの語に置き換える。**正本は要件定義（FEAT/状態）**とし、状態イベントは `30_データ・IF設計/03_ドメインイベント.md`、外部照会IFは `03_外部連携IF/README.md` を参照。関連DECを併記する。

## 目次
1. [概要](#1-概要)
2. [処理ロジック](#2-処理ロジック)
3. [例外・リトライ・多重制御](#3-例外リトライ多重制御)
4. [状態確定と結果反映](#4-状態確定と結果反映)
5. [パラメータ](#5-パラメータ)

## 1. 概要
> 📝 ここにバッチの実行基盤・起動条件・対象・目的を記載。{実行基盤（スケジューラ→ワーカー等）／起動間隔（DEC-__）／処理対象（抽出条件）／目的／多重制御方式}

| 項目 | 内容 |
|---|---|
| バッチ名 |  |
| 実行基盤 |  |
| トリガ（起動間隔） |  |
| 処理対象（抽出条件） |  |
| 目的 |  |
| 多重制御 |  |

## 2. 処理ロジック
> 📝 ここに1回の起動で行う処理を擬似コード/箇条書きで記載。対象抽出→各対象の処理分岐→状態遷移→次回への持ち越し、を明示する。%% ここに記載（実データは書かない）

```
{スケジューラ}({間隔}) → {処理}ワーカー:
  対象 = {テーブル/キュー} WHERE {未終端抽出条件}
  各対象:
    {排他フラグ}=true（多重ディスパッチ防止）
    {外部照会/処理} → {結果分岐}:
      {成功条件}   → {状態A}, {結果反映}
      {失敗条件}   → {状態B}, {結果反映（理由付き）}
      {継続条件}   → {カウンタ}++ , {排他フラグ}=false（次回へ）
    {上限条件} → {タイムアウト処理/ERR-__}
  スタック救済: {排他フラグ}=true が {しきい値} 放置 → false へリセット
```

## 3. 例外・リトライ・多重制御
> 📝 ここに例外事象ごとの判定条件・振る舞い・状態/エラーを記載。{事象／判定条件／振る舞い／ST・ERR}

| 事象 | 判定 | 振る舞い | ST/ERR |
|---|---|---|---|
| {結果待ちタイムアウト} | {上限到達条件（DEC-__）} | {拾い直し/隔離＋通知} | ERR-_ → ST-_ |
| {多重ディスパッチ} | {同一対象の並行処理} | {排他フラグで排他} | — |
| {処理中スタック} | {排他フラグが長時間放置} | {リセットで救済} | — |
| {継続据え置き} | {処理中ステータス} | {カウンタ++して次回へ} | ST-_ 保持 |

- 間隔・回数の根拠: {実測/外部仕様の踏襲根拠}。{確定を裏取りする材料}。
- リトライ対象/非対象: {一時失敗＝再送 / 永続失敗＝再送しない の区別}

## 4. 状態確定と結果反映
> 📝 ここに終端状態への確定と結果反映の流れを記載。冪等（状態ガード・許可遷移のみ・終端不変）で二重反映を防ぐ点を明示する。{結果／遷移ID／反映先／冪等の担保}

- {成功結果} → **{終端状態A}**（ST-T__）: {結果反映先・後続処理}。
- {失敗結果} → **{終端状態B}**（ST-T__）: {結果反映（理由付き）／再送対象か}。
- 結果反映は{担当サービス}側で**状態ガード**（許可遷移のみ・終端は不変）により1回だけ適用（冪等）。

## 5. パラメータ
> 📝 ここにバッチの調整パラメータと根拠（DEC-ID）を記載。{パラメータ／値／根拠}

| パラメータ | 値 | 根拠 |
|---|---|---|
| {起動間隔} |  | DEC-_ |
| {最大リトライ/ポーリング回数} |  | DEC-_ |
| {スタック救済しきい値} |  |  |
| {再送回数N・バックオフ} |  | DEC-_ |

> 関連: 外部照会IF＝`03_外部連携IF/README.md` / 状態イベント＝`30_データ・IF設計/03_ドメインイベント.md` / 監視（滞留アラート）＝要件定義（非機能要件）。
