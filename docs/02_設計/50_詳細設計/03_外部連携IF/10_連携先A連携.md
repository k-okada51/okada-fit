---
status: draft
---

# {連携先A} 連携 — EXT-__

> {連携先A} の連携手順・status写像・識別子ライフサイクル。**外部ワイヤ（既存API契約）は改変しない**。直列化・冪等・再送などの共通非機能は [`README.md`](README.md#共通-連携の直列化冪等再送) を参照。

## 1. {連携先A} 連携手順（EXT-__）
> 📝 ここに外部API呼び出しの手順（複数ステップの連携シーケンス）を記載。認証ヘッダ・文字コード・改変禁止の前提を冒頭に明記。%% ここに記載（実データは書かない）

```
{起点イベント/enqueue}(EXT-__) → {ワーカー}:
  ① {手順1: 入力データ組立}   ② {手順2: 外部POST} → {返却ID}
  ③ {手順3} → {識別子を保存}
  ④ {手順4: 原稿/本体入稿}    ⑤ {手順5: 実行発火}
  ⑥ {手順6: 初期照会} → {初期status}
  → {ログ}: {識別子}保存・status={状態}（ST-_）・{後続反映}
```

> 📝 ここに各手順のエンドポイント・内容・記録先を表で記載。認証は `{Authorizationヘッダ形式}`（機密はSecrets管理）。{#／手順／エンドポイント／内容／記録先}

| # | 手順 | エンドポイント | 内容 | 記録 |
|---|---|---|---|---|
| ① |  | （内部） |  | — |
| ② |  | `{METHOD} /{path}` |  |  |
| ③ |  | `{METHOD} /{path}` | **{識別子}を採番・保存** |  |
| ④ |  | `{METHOD} /{path}/{id}/...` |  |  |
| ⑤ |  | `{METHOD} /{path}/{id}/...` |  |  |
| ⑥ |  | `GET /{path}/{id}` |  |  |

- 照会: `GET /{path}/{id}` → {返却されるstatus・件数フィールド}。ポーリングは `../02_バッチ設計.md`。
- エラー詳細の所在: {エラー詳細がどの階層に返るか（本体レベル等）／併せて判断する項目}

## 2. {連携先A} status → 本PJ status 写像 `[仮]`
> 📝 ここに外部システムのstatus値を本PJの状態（ST）へ写像する表を記載。正本は `01_DB物理設計.md` のenum・`30_データ・IF設計/03_ドメインイベント.md`。{外部status／区分／本PJ status／遷移ID}

| {連携先A} status | 区分 | 本PJ status | 遷移 |
|---|---|---|---|
|  | 進行中 |  | 据え置き |
|  | 成功 |  | ST-T_ |
|  | 失敗 |  | ST-T_ |

## 3. {識別子} ライフサイクル
> 📝 ここに外部連携で払い出される識別子（照会キー）の採番→保存→照会→終端までのライフサイクルを記載。突合キーと照会キーが別なら二本立てである旨を明示。

`{識別子}` は{手順③}で採番→`{テーブル}.{列}` に保存→{照会・確認}の**照会キー**→終端（{終端状態}）で確定。突合は `{突合キー}`、外部照会は `{識別子}` の**二本立て**。

> 関連: 共通・他の連携先＝[`README.md`](README.md) / 契約スキーマ＝`30_データ・IF設計/02_API設計.md` / ポーリングバッチ＝`../02_バッチ設計.md`。
