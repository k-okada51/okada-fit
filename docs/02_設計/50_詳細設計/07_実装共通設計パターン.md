---
status: draft
---

# 実装共通設計パターン — {システム名}

> **目的**: 全機能の実装が従う横断的な共通方針——エラー処理・トランザクション境界・冪等性・リトライ・多重制御——を1枚で固定し、機能ごとの実装ブレをなくす。
> **書き方**:（記入例は example-suido-fax の対応ファイル参照）実データは書かず、`{ }` を自プロジェクトの語に置き換える。エラー母集合（ERR）・状態機械（ST）の正本は `60_テスト設計/02_RED母集合_受入基準・状態・エラー.md`、共通エラー契約は `30_データ・IF設計/02_API設計.md` を参照し矛盾させない。決定は関連DEC/ADRを併記する。

## 目次
1. [エラー処理](#1-エラー処理)
2. [トランザクション境界](#2-トランザクション境界)
3. [冪等性](#3-冪等性)
4. [リトライ](#4-リトライ)
5. [多重制御・排他](#5-多重制御排他)

## 1. エラー処理
> 📝 ここに共通のエラー処理方針を記載。{例外の分類（業務エラー/システムエラー/一時失敗）／ERR-IDへのマッピング／共通エラー応答契約（`02_API設計.md`）との対応／ログ出力（`05_ログ設計.md`）／利用者向けメッセージの出し方／握り潰し禁止の原則}。

| 分類 | 判定 | 振る舞い | ERR/ログ |
|---|---|---|---|
| 業務エラー | （記入） | （記入） | ERR-_ |
| システムエラー | （記入） | （記入） | ERR-_ |
| 一時失敗 | （記入） | （記入・リトライ対象） | ERR-_ |

## 2. トランザクション境界
> 📝 ここにトランザクション境界の原則を記載。{1ユースケース＝1トランザクションの原則／境界の単位（サービス/ユースケース）／外部呼び出しをトランザクション内に含めない方針／分散時の整合（Outbox・Saga等）／状態遷移の永続化タイミング}。

- 境界の単位: {記入}
- 外部I/Oの扱い: {トランザクション外／補償 の方針}
- 分散整合: {Outbox/Saga 等・関連DEC-__}

## 3. 冪等性
> 📝 ここに冪等性の担保方針を記載。{冪等キーの発行・伝播（`02_API設計.md` の共通ヘッダ）／状態ガード（許可遷移のみ・終端不変）による二重反映防止／重複リクエストの検知・棄却／リトライ安全な操作設計}。二重処理が発生しないことが読み取れるように。

| 対象操作 | 冪等の担保方式 | 根拠 |
|---|---|---|
| （記入） | 冪等キー / 状態ガード | DEC-_ |

## 4. リトライ
> 📝 ここにリトライの共通方針を記載。{リトライ対象（一時失敗）と非対象（永続失敗）の区別／回数・バックオフ（指数/ジッタ）／上限到達時の扱い（隔離・通知 ERR-__）／外部システムのレート制限との整合}。値の根拠（DEC-ID）を併記。

| パラメータ | 値 | 根拠 |
|---|---|---|
| リトライ対象 | （一時失敗のみ） | （記入） |
| 最大回数 | （記入） | DEC-_ |
| バックオフ | （記入） | DEC-_ |
| 上限到達時 | （隔離・通知 ERR-_） | （記入） |

## 5. 多重制御・排他
> 📝 ここに同時実行・多重処理の制御方針を記載。{直列化が必要な処理と同時実行の上限／排他の手段（分散ロック・FIFOキュー・処理中フラグ）／多重ディスパッチ防止（`is_processing` 等）／スタック検知と救済／冪等（§3）との役割分担}。関連DEC-__。

- 直列化・排他の対象: {記入（例: 外部送信は同時実行=1）}
- 手段: {FIFOキュー / 分散ロック / 処理中フラグ 等}
- スタック救済: {タイムアウト検知・再ディスパッチの方針}

> 関連: 共通エラー契約＝`30_データ・IF設計/02_API設計.md` / ERR・ST母集合＝`60_テスト設計/02_RED母集合_受入基準・状態・エラー.md` / バッチのリトライ・多重制御＝`02_バッチ設計.md` / ログ＝`05_ログ設計.md`。
