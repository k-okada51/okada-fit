---
status: draft
---

# 技術選定 — okada-fit

> **目的**: 各層でどの技術を採用したか（言語・実行基盤・DB・非同期・保管・監視・IaC 等）と、その採用理由を固定する。
> **書き方**:（記入例は example-suido-fax の対応ファイル参照）実データは書かず、層／採用技術／理由のみ記す。採用の確定は **ADR（`../../意思決定_ADR/`）** 経由で、`[仮]`・🔧DESIGN を実装で勝手に確定しない。

## 目次
1. [選定サマリー](#1-選定サマリー)
2. [共通スタック](#2-共通スタック)
3. [サービス別スタック](#3-サービス別スタック)
4. [DB選定の要点](#4-db選定の要点)
5. [主要な採用理由（横断）](#5-主要な採用理由横断)
6. [技術選定に紐づく未決（DEC）](#6-技術選定に紐づく未決dec)

## 1. 選定サマリー
> 📝 ここに層ごとの採用を一言理由付きで記載。{領域（言語/実行基盤/DB/非同期/保管/監視/IaC/リージョン等）／採用技術／一言理由}

| 領域 | 採用技術 | 一言理由 |
|---|---|---|
| 言語 | TypeScript `[仮]` | フロント/サーバを1言語で、型安全 |
| フレームワーク | Next.js（App Router）`[仮]` | フルスタックWeb、Vercel無料枠と親和（DEC-D01） |
| UI | shadcn/ui（Radix UI＋Tailwind CSS） | 画面モック規約で採用済み |
| DB | Supabase（PostgreSQL・無料枠）`[仮]` | 無料枠DB採用（DEC-B03） |
| ホスティング | Vercel（無料枠）`[仮]` | Next.js親和・無料枠 |
| AI | Claude API（`claude-opus-4-8` `[仮]`・画像入力対応） | AI採用＝Claude確定（DEC-B01）、モデルtierはDEC-D02 |
| 認証 | Supabase Auth `[仮]` | 単一ユーザだが公開面の本人限定（DEC-D03） |
| 画像 | 食事写真はClaudeへ画像入力で送信 `[仮]` | 永続化要否はDEC-D04 |

## 2. 共通スタック
> 📝 ここに全サービス共通のスタックを記載。{領域／採用／備考}

| 領域 | 採用 | 備考 |
|---|---|---|
| ランタイム | Node.js（Next.js サーバ/API routes）`[仮]` | AI呼び出しはサーバ側のみ |
| 通信 | HTTPS/TLS | ブラウザ↔アプリ、アプリ↔Claude/DB |
| シークレット | Vercel/Supabase の環境変数 | Claude APIキー・DB資格情報。コード直書き禁止 |

## 3. サービス別スタック
> 📝 ここにサービスごとの採用を記載（サービスが1つなら本節は共通スタックに統合可）。{領域／採用／備考}

### 3.1 okada-fit-web（単一Webアプリ）
| 領域 | 採用 | 備考 |
|---|---|---|
| フロント | Next.js/React + shadcn/ui `[仮]` | 画面はSCR-01〜05（人間承認前） |
| サーバ | Next.js API routes `[仮]` | 絞り込み・残量算出・Claude呼び出し |
| DB接続 | Supabaseクライアント `[仮]` | サーバ側から接続 |

> 📝 本PJはサービスが1つのため §3.2 は不要（省略）。

## 4. DB選定の要点
> 📝 ここにデータストア選定の理由を記載。分離方針（サービス別/用途別）と各DBの向き不向き。{DB／対象データ／採用理由}

| DB | 対象データ | 採用理由 |
|---|---|---|
| Supabase / PostgreSQL `[仮]` | 器具・部位タグ／記録／プロフィール／食事マスタ／食事記録 | 無料枠あり（DEC-B03）、リレーショナルで整合性を担保、単一DBで十分な規模 |

> ⚠️ 要確認（人間判断）: 具体プロダクトは「無料枠であること」を条件に確定（DEC-B03）。Supabase以外（Neon 等）も候補。実装時に選定。

## 5. 主要な採用理由（横断）
> 📝 ここに層をまたぐ主要な採用理由を記載。{技術選定が満たす要件（NFR/REQ）と設計意図}

- **1言語・単一Webアプリ**: 個人開発（実装者＝岡田さん）で保守負担を最小化。フロント/サーバをNext.jsに集約。
- **無料枠中心**: ホスティング（Vercel）・DB（Supabase）とも無料枠。コスト制約（予算未確定）に配慮。
- **AIはサーバ側**: Claude APIキーをクライアントに露出させない（境界・DEC-B01）。
- **決定的処理とAIの分離**: 絞り込み・不足分抽出はDB照会（AI不使用）、生成のみClaude。

## 6. 技術選定に紐づく未決（DEC）
> 📝 ここに技術選定に効く未決事項を記載。確定は ADR 経由。{DEC-ID／決定事項／推奨方針・候補／ステータス}

| DEC | 決定事項 | 推奨方針/候補 | ステータス |
|---|---|---|---|
| DEC-D01 | Webフレームワーク | Next.js（App Router）+ TypeScript + shadcn/ui | `[仮]`（おまかせ提案） |
| DEC-D02 | 採用モデルの tier | `claude-opus-4-8` を既定。コスト重視なら Sonnet/Haiku も検討（画像入力対応を維持） | 未決定 |
| DEC-D03 | 認証方式 | Supabase Auth 等で本人限定 | ⚠️ 要確認 |
| DEC-D04 | 食事写真の外部送信・保持 | 送信は前提。保存要否・保持期間を確定 | ⚠️ 要確認 |
| DEC-B03 | DB具体プロダクト（無料枠） | Supabase/PostgreSQL を既定候補 | `[仮]` |

> 関連: 構成上の配置＝`01_構成要素.md` / 外部連携＝`04_外部連携.md` / 採用の確定＝`../../意思決定_ADR/`。
