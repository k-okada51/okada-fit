'use client';

import { createTheme } from '@mantine/core';

// okada-fit テーマ: アクセントカラー teal（コンテンツ設計より）
export const theme = createTheme({
  primaryColor: 'teal',
  defaultRadius: 'md',
  fontFamily:
    '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Hiragino Sans", "Noto Sans JP", sans-serif',
});
